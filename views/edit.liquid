<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>{{ title }}</h1>
        <form action="{{ actionUrl }}" method="post">
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="{{ post.title | default: '' }}" required>
            <label for="content">Content:</label>
            <textarea id="content" name="content" required>{{ post.content | default: '' }}</textarea>
            <label for="color">color:</label>
            <input type="text" id="color" name="color" value="{{ post.color | default: '' }}" required>
            <button type="submit">{{ buttonText }}</button>
        </form>
        <a href="/">Back to home</a>
    </div>
</body>
</html>
